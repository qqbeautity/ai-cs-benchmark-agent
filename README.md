# AI 客服竞品横评 Agent

输入 3–5 个 AI 客服产品，自动联网调研，生成带证据来源、对比矩阵与 AI 洞察的横评报告。

**项目状态：骨架完成，示例路径可演示，真实调研链路尚未验证。**

> 这份 README 是**项目现状说明**，不是成品宣传。最后更新：2026-09-14。
> 面向面试官的精简版和完整技术方案见文末的「相关文档」。

---

## 一、这个项目在解决什么问题

让大模型做竞品调研，最大的坑不是它写得不好，而是它**会自信地编造**：

- 编造一个根本不存在的价格
- 编造一个它"记得"的客户数量
- 最隐蔽的：编造一条**看起来有来源、实际查无此处**的引用

前两种容易被发现，第三种最难——报告里写着"已核实事实"、旁边挂着 `[S3]` 标签，读者点不开或者点开发现对不上，整份报告的可信度就塌了。

所以这个项目的核心不是"让 AI 写报告"，而是**让 AI 写的每一句话都可追溯，且这个可追溯是程序保证的，不是靠提示词祈祷的**。

---

## 二、现在能做什么 / 不能做什么

### ✅ 已验证可用

| 能力 | 验证方式 |
|---|---|
| 示例报告完整展示（9 个区块） | Playwright 冒烟测试，浅色+深色各跑一遍 |
| 十个维度热力矩阵 | 截图见 `docs/screenshots/` |
| 证据覆盖率条形图（ECharts） | 同上，含深色模式配色重新取色 |
| 定价模式表格 | 同上 |
| 三级证据标注 + 来源列表 | 同上 |
| 创建任务页表单校验 | 冒烟测试覆盖（少于 3 个产品会拦） |
| 低于 3 个产品、无任务时的空状态 | 冒烟测试覆盖 |
| **live 路径在没有 API Key 时降级渲染** | `scripts/smoke-live.mjs`：种入任务记录后渲染 `/report`，断言 9 个区块齐全且无控制台报错 |
| 来源绑定校验逻辑 | **13 个单元测试** |
| 指数退避重试逻辑 | **7 个单元测试** |
| 示例数据结构完整性 | **8 个单元测试**（整份 fixture 过一次 schema） |
| 信息缺口的文案组装 | **5 个单元测试** |
| 生产构建 / 类型检查 | `next build` 与 `tsc --noEmit` 均通过 |

**当前测试总数：33 个单元测试 + 3 个浏览器冒烟脚本，全部通过。**

### ❌ 还没验证 / 还没做

| 缺口 | 说明 | 严重程度 |
|---|---|---|
| **真实调研链路从未跑通** | 全程没填过 API Key，Tavily 实际抓取、Gemini 实际抽取、真实来源绑定的行为**全部未知** | 🔴 最高 |
| 示例报告是**占位数据** | 产品全是虚构的，只用来验证渲染路径。跑一次 `npm run fixture` 就会换成真实数据 | 🔴 高 |
| 没有部署 | 还是本地项目，没有公开链接 | 🟡 中 |
| **不是 git 仓库** | 没有版本控制，`git init` 都没做 | 🟡 中 |
| `npm run lint` 是坏的 | 脚本存在，但没有 ESLint 依赖和配置，跑起来直接失败 | 🟢 低 |
| 服务端模块没有测试覆盖 | 单测只覆盖了客户端安全的模块，`research.ts` / `gemini.ts` / `tavily.ts` 无测试 | 🟡 中 |
| 人工核查正确率未做 | PLAN.md 要求抽查 20 条关键事实记录正确率，还没做 | 🟡 中 |

> ⚠️ **重要**：因为示例报告是虚构数据，页面上有一条橙色横幅明确标注「这是占位示例，不是真实调研结果」。**在做完真实调研之前，不要把这个链接当作成果展示。**
>
> 横幅由 fixture 文件里的 `_placeholder: true` 驱动。跑一次 `npm run fixture` 生成真实数据后，这个字段会消失，横幅也就不再显示——所以它不会在你已经做完整调研之后还挂着，也不会在还是假数据时消失。

---

## 三、怎么跑起来

```bash
npm install
npm run dev        # 默认 3000 端口
```

打开 http://localhost:3000 ，点首页的「**查看示例报告（无需 API Key）**」——**这条路径不发起任何联网请求**，可以立刻看到报告的完整形态。

### 想看真实调研？需要先配两个免费 Key

| 服务 | 申请地址 | 免费额度 |
|---|---|---|
| Tavily（联网搜索） | https://app.tavily.com/ | 1000 credits / 月 |
| Gemini（结构化抽取） | https://aistudio.google.com/apikey | 约 10–30 RPM，按账号浮动 |

两个都**不需要信用卡**。

```bash
cp .env.example .env.local
# 编辑 .env.local 填入两个 Key
npm run dev
```

**额度换算**：一次完整演示（5 产品 × 3 次搜索）= 15 credits，1000 ÷ 15 ≈ **每月 66 次**。

> ⚠️ Gemini 免费层下 Google 可能将输入用于改进产品。本项目只处理公开网页，**不要输入企业机密**。

---

## 四、技术方案一句话版

```
产品名 + 官网（可选）
        ↓
   Tavily 搜索（每产品最多 3 次，官网域名优先）
        ↓
   来源编号化 [S1] [S2] [S3] ...    ← 只有编号进模型，URL 不进
        ↓
   Gemini 按十维结构化抽取（所有产品合并成 1 次调用）
        ↓
  ★ 程序校验：模型引用的 sourceId 真的存在吗？    ← 项目的核心
        ↓
   查无此引用 → 该条事实降级为「待确认」
        ↓
   热力矩阵 + 覆盖率图 + 定价表 + 三级证据标注
```

**为什么只让编号进模型？** 因为一旦 URL 进了上下文，模型就会开始"修饰"它——补全路径、改写成看起来更权威的域名。编号是它可以引用、但无法伪造的。

**校验规则**（`src/lib/schema.ts`，文案与降级说明在 `src/lib/types.ts`）：

- 引用了不存在的 sourceId → 降级为「待确认」，并在界面上说明**为什么**降级
- 某个维度声称有能力、但所有引用都失效 → 该维度**退回「未知」且摘要清空**（有摘要没证据 = 读起来像已确认事实）
- 模型写进正文的 URL → 一律替换成 `[链接见来源]`，页面只渲染我们亲自抓取并校验过的链接

---

## 五、十个分析维度

产品定位与目标客户 · 客服接入渠道 · AI Bot 与自动回复 · 知识库与 RAG · 人工接管与坐席辅助 · 工作流和自动化 · 数据分析 · 集成能力 · 安全与部署 · 定价及试用政策

每个维度四种状态，**同时带图标和文字**（不依赖颜色区分，色盲 / 灰度打印 / 强制配色模式下都可读）：

| 状态 | 图标 | 判定标准 |
|---|---|---|
| 支持 | ✓ | 摘录明确说明该能力存在 |
| 部分支持 | ◐ | 存在但有限制、需额外付费、或仅部分渠道 |
| 不支持 | ✕ | 明确不支持，或由第三方提供 |
| 未知 | ? | 摘录未提及或说法冲突 |

每条结论另带三级证据标注：**已核实事实**（可点开来源）/ **AI 推断**（跨事实归纳）/ **待确认**（资料不足或冲突）。

---

## 六、代码结构

```
src/
├── app/
│   ├── page.tsx                  创建任务（首页）        214 行
│   ├── research/page.tsx         调研进度 + 单产品重试    240 行
│   ├── report/page.tsx           报告页（9 个区块）       506 行
│   └── api/research/
│       ├── product/route.ts      单产品调研               40 行
│       └── report/route.ts       横评洞察                 30 行
├── components/
│   ├── dimension-matrix.tsx      十维热力矩阵            119 行
│   ├── coverage-chart.tsx        ECharts 覆盖率图        134 行
│   ├── claim-list.tsx            证据标注 + 来源列表      108 行
│   └── theme-toggle.tsx          深浅色切换               41 行
├── lib/
│   ├── types.ts                  ★ 域模型 + 共享推导      313 行
│   ├── schema.ts                 ★ 来源绑定与校验         139 行
│   ├── gaps.ts                   信息缺口文案（可测）      33 行
│   ├── retry.ts                  指数退避                 77 行
│   ├── tavily.ts                 Tavily 客户端           143 行
│   ├── gemini.ts                 Gemini 客户端           128 行
│   ├── prompts.ts                所有提示词               149 行
│   ├── research.ts               单产品编排              178 行
│   ├── report.ts                 横评合成                124 行
│   ├── fixture.ts                示例数据装载             60 行
│   ├── server-env.ts             服务端配置（server-only） 43 行
│   ├── task-store.ts             localStorage             58 行
│   └── validate-result.ts        接口边界校验            160 行
└── fixtures/
    └── shape-example.json        占位示例（虚构产品）
```

**合计约 3850 行**（含测试与脚本）。

### 一个函数，两个调用方

产品卡片（优势 / 短板 / 适用用户 / 定价模式）由 `types.ts` 里的 `deriveProductInsights` **统一产出**。区别只在于传不传模型散文：服务端 `report.ts` 传 Gemini 归纳的结果，客户端在没有洞察时（降级、占位示例）不传，函数自己回退到从已验证事实里推导。

以前这里有两份实现——报告页复制过一份服务端定价推导，两边各自演化。现在只有一份，且它躺在客户端安全的模块里，所以两边都能用。

### 一条重要的结构规则

`src/lib/server-env.ts` 带 `import "server-only"`。**任何客户端组件都不许直接或间接引用它**——一旦引用，Next.js 会在**构建期**报错。

这不是洁癖。项目第一次跑起来时就是这么白屏的：`report/page.tsx` 从 `report.ts` 导入了一个叫 `isDone` 的类型守卫，而 `report.ts` 依赖 `server-env.ts`，结果整个服务端模块被拖进浏览器包，模块顶层守卫抛错，报告页整页崩溃。

修复方式是把 `isDone` 挪到 `types.ts`（它本来就是个纯类型守卫）。**如果客户端要用服务端模块里的东西，把它挪到 `types.ts`，而不是放宽边界。**

---

## 七、命令

```bash
npm run dev            # 开发服务器（3000 端口）
npm run build          # 生产构建
npm run typecheck      # 类型检查
npm test               # 全部单元测试（33 个）
npx vitest run src/lib/schema.test.ts      # 只跑一个文件
npx vitest run -t "降级"                    # 按测试名跑单个用例

npm run smoke          # 三个浏览器冒烟脚本 + 截图
npm run fixture        # 用真实 API 生成示例报告（会拒绝覆盖占位数据，除非带 --force）
```

**注意事项：**

- `npm run smoke` 需要 dev server 跑在 **3111** 端口（`npx next dev -p 3111`），脚本里硬编码了这个端口
- 三个冒烟脚本：`smoke-home.mjs`（首页与表单校验）、`smoke.mjs`（占位报告，浅色+深色各一遍）、`smoke-live.mjs`（真实路径：种入任务记录，验证没有 API Key 时报告依然能降级渲染）
- 截图输出到 `docs/screenshots/`
- `npm run lint` **是坏的**，没有 ESLint 配置，用 `npm run typecheck` 代替
- 报告页是客户端组件，**`curl` 只能拿到 loading 态**，验证内容必须用 Playwright 冒烟测试

---

## 八、一些刻意的设计取舍

| 决策 | 原因 |
|---|---|
| 十维用**热力矩阵**而非分组柱状图 | 5 产品 × 10 维度 = 50 个比较项，柱状图完全不可读；而且这些是**状态值不是数值** |
| 定价用**表格**而非柱状图 | 多数厂商不公开可比价格，强行画图等于造假。只有当所有产品都有真实数字时才画图 |
| 洞察生成失败**不影响报告** | 矩阵和图表都基于已验证事实构建，洞察是可降级的附加层 |
| 调研**串行**而非并发 | 免费层限流下，429 风暴比慢一点更糟。这是有意设定的上限策略，不是"还没优化到的顺序代码" |
| 依赖只留 5 个 | 曾引入 `@tanstack/react-query`，但它只包了两个 `useState` 就能表达的请求。删掉后报告页首屏少 33 KB（gzip 后 10.8 KB） |
| 失败也返回 **HTTP 200** | 响应体是判别联合类型，前端要靠它渲染重试按钮。非 200 只表示请求本身格式错误 |
| 不建数据库 | 作品集 MVP，静态 fixture + localStorage 够用。完整报告不存 localStorage（5 产品×10 维度会超过 5MB 上限） |

---

## 九、相关文档

| 文件 | 内容 | 给谁看 |
|---|---|---|
| `PLAN.md` | 完整技术方案 v2，含成本核算、免费额度分析、修改原因 | 想了解设计决策全过程 |
| `AGENTS.md` | 架构约束、模块边界、已知陷阱 | 要改这份代码的人 |
| `docs/screenshots/` | 浅色/深色截图各 3 张 | 快速看效果 |

三个都保留，内容有重叠但侧重不同。

---

## 十、下一步

按优先级：

1. **配 API Key，跑通一次真实调研** ← 当前唯一阻塞项
2. `npm run fixture` 生成真实示例，替换掉占位数据（横幅会自动消失）
3. **`git init` + 首次提交**
4. 部署到 Vercel（见下）
5. 人工核查 20 条关键事实，记录正确率
6. 补服务端模块的测试

### 部署说明

目标平台是 **Vercel**，不是 Netlify。

原因：单产品链路耗时 30–90 秒，而 **Netlify 的同步函数上限是 60 秒，且官方明确该限制不随套餐变化、不可配置**（升级付费也没用）。Vercel Hobby 在 Fluid Compute 下是 300 秒，足够。

部署前在 Vercel 项目设置里配置环境变量：`TAVILY_API_KEY`、`GEMINI_API_KEY`、`GEMINI_MODEL`。
