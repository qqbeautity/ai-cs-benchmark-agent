# AI 客服竞品横评 Agent：MVP 方案（v2）

> v2 修订说明：本版根据部署平台超时限制、免费层限流现实、以及"演示必须零 API 调用也能跑"三条约束重构。
> 相比 v1 的关键改动集中在 §3 技术方案与 §4 排期，产品定义（§1、§2）基本沿用。

## 1. 产品定义

面向 AI 产品经理和求职者，用户输入任意 3–5 个 AI 客服产品及可选官网地址，系统自动联网调研，并生成一份带证据来源、对比表、图表和 AI 洞察的 HTML 横评报告。

首版成功标准：

- 完整跑通"输入产品 → 联网搜索 → 信息抽取 → 横向比较 → HTML 报告"；
- 至少用一组真实 AI 客服产品完成稳定演示；
- 关键事实均展示来源，无法确认的信息明确标记；
- **零 API Key 时可查看预置示例报告**（见 §3.6，这是演示可靠性的底线）；
- 报告适配电脑端，可通过公开链接用于简历展示；
- 不承诺任意网站都能抓取成功，不做生产级通用爬虫。

## 2. MVP 功能与页面

### 创建任务页

用户填写：

- 调研目的，默认"AI 客服产品选型与市场机会分析"；
- 3–5 个产品名称；
- 每个产品的官网地址，可选但推荐；
- 是否允许引用第三方来源，默认开启（原因见 §3.7）。

分析维度固定为十项：

1. 产品定位与目标客户
2. 客服接入渠道
3. AI Bot 与自动回复
4. 知识库与 RAG
5. 人工接管与坐席辅助
6. 工作流和自动化
7. 数据分析
8. 集成能力
9. 安全与部署
10. 定价及试用政策

### 调研进度页

按产品显示状态：

`等待 → 搜索中 → 抽取中 → 已完成 / 信息不足 / 失败`

支持单个产品重试。某个产品失败时继续生成其余产品报告，不让整个任务中断。

### 报告页

1. 调研范围、时间及来源规则；
2. 产品定位摘要卡片；
3. 十维能力对比**热力矩阵**；
4. **证据覆盖率**条形图；
5. 定价：可比较时展示价格图，不可比较时改为定价模式表；
6. 各产品优势、短板与适用用户；
7. 市场共性、差异化和潜在机会；
8. 信息缺口及待人工验证项；
9. 按产品整理的来源列表。

每个结论标记为：

- `已核实事实`：有网页证据；
- `AI推断`：根据多个事实归纳；
- `待确认`：公开资料不足或互相冲突。

首版不做报告内继续对话、账号系统、历史同步、定时监控和多人协作。

### 图表形式说明（v2 调整）

> v1 的"十维能力对比表 + 功能覆盖柱状图"在图表选型上不成立：5 个产品 × 10 个维度 = 50 根柱子，分组柱状图会完全不可读。
> 十维是**状态型数据**（支持 / 部分支持 / 不支持 / 未知），不是数值型，因此改用**热力矩阵**（产品 × 维度网格）。

| 报告区块 | 采用形式 | 理由 |
|---|---|---|
| 十维能力对比 | **热力矩阵**（行 = 产品，列 = 维度，单元格 = 四态色块 + 图标 + 文字） | 状态型数据；50 个条目在网格中可读，在柱状图中不可读 |
| 功能覆盖度 | **横向条形图**（每产品一条，"已确认支持的维度数 / 10"） | 这才是真正的数值比较，单一数值序列用顺序色 |
| 定价 | 可比较 → 柱状图；不可比较 → 表格 | 多数 AI 客服产品是"联系销售"报价，强行画图会造假 |
| 证据质量 | **状态标签**（图标 + 文字） | 状态色，不参与分类色序 |

四态映射到固定状态色（**永不用于数据系列**），且每个色块**必须同时带图标和文字**，不允许颜色单独承载语义：

| 状态 | 色值 | 图标 |
|---|---|---|
| 支持 | `#0ca30c` | ✓ |
| 部分支持 | `#fab219` | ◐ |
| 不支持 | `#d03b3b` | ✕ |
| 未知 | 灰 `#898781` | ? |

## 3. 技术与数据方案

### 3.1 技术栈

- **Next.js 16 + TypeScript**：前端与 API Routes；
- **Tailwind CSS 4**：界面；
- **ECharts 6**：图表；
- ~~**@tanstack/react-query**：前端并发调度、重试、缓存~~ —— **已废弃**（实现后评估：这两个请求用 `useState` 就能表达，react-query 只贡献了 33 KB 的包体）；
- **zod**：模型输出的运行时校验；
- **Gemini API**：规划、结构化抽取和报告归纳；
- **Tavily Search API**：联网搜索；
- **Vercel**：公开部署（**v1 的 Netlify 已废弃，原因见 §3.2**）；
- **localStorage**：仅保存最近一次任务的任务元数据；报告正文走静态 fixture（§3.6）。

不引入数据库，不引入多 Agent 框架。

### 3.2 部署平台：为什么必须是 Vercel

这是 v1 最大的硬伤。

v1 让前端对每个产品并发调用一个 API Route，而单个产品的链路是「3 次搜索 → 抓取网页 → 大模型抽取 → 校验」，**实测 30–90 秒**。

- **Netlify 同步函数上限 60 秒，且官方明确该限制不随套餐变化、不可配置** —— 升级付费也无法解决。超时即请求被切断，前端只能收到一个无意义的错误。
- Netlify 的 background function（15 分钟）可以绕开，但需要改成「立即返回 → 前端轮询任务状态」，等于凭空多写一套任务状态机。
- **Vercel Hobby（免费）在 Fluid Compute 下函数上限 300 秒**，足够单产品链路跑完。且 Hobby 默认即 300 秒，无需配置。

因此部署平台改为 Vercel。若日后必须迁回 Netlify，需要把 `/api/research/product` 改造为 background function + 轮询。

### 3.3 Gemini 免费层限流对策

免费层大致为 **10–30 RPM、500–1500 RPD，按账号浮动**。v1 的设计里每个产品要调 2 次模型（十维抽取 + 洞察），5 个产品 = 10 次调用，叠加开发期反复重试，**极易触发限流**，而限流会被用户看成一个普通报错。

三条对策：

1. **合并调用**：5 个产品的十维抽取**合并为 1 次模型调用**（5 份网页摘要一次性喂入，输出数组）。既省配额，又让横向对比在同一上下文里完成、口径更一致。
2. **指数退避重试**：`429 / 5xx` → 等 2s、4s、8s 重试最多 3 次，每次记日志。**这条不写，演示时会当场卡住。**
3. **模型名走环境变量**：`GEMINI_MODEL`，默认免费层可用的轻量模型。免费层的模型可用性是会变的，硬编码会在某天突然全挂。

> ⚠️ 免费层下 **Google 可能将输入用于改进产品**。首版禁止输入企业机密；作品集演示一律只用公开信息。

### 3.4 Tavily 额度核算

5 产品 × 3 次搜索 = **15 credits / 次完整演示**。免费层 1000 credits/月 ÷ 15 ≈ **每月 66 次完整演示**。

开发期调试烧得快（每次重跑都计费），因此：

> **开发阶段用 Claude Code 内置的 WebSearch / WebFetch 验证「搜索 → 抽取」链路，完全不消耗 Tavily 额度。** 等链路调通、Prompt 稳定后，再切换到真实 Tavily 调用做端到端验证。

### 3.5 调研工作流

采用"确定性工作流 + 局部 Agent 判断"，不搭建多 Agent 框架：

1. 根据产品名和官网生成固定搜索问题；
2. 每个产品执行最多 3 次基础搜索；
3. 官网、官方帮助中心、官方定价页优先；
4. 抽取网页摘要、URL、标题和访问时间；
5. 模型按统一 JSON Schema 抽取十个维度（**5 个产品合并为 1 次调用**）；
6. 程序校验字段和引用关系（zod）；
7. 模型只基于结构化事实生成洞察；
8. 前端根据 JSON 渲染报告和图表。

所有模型提示都要求：

- 不得用常识补全缺失事实；
- 每项事实必须绑定至少一个来源 ID；
- 企业报价、准确率、客户数量等无法核实时返回 `unknown`；
- 事实与推断分开输出。

**来源绑定是这类项目最容易反复调试的部分**——模型会自信地编造 `source_id`。因此第 6 步的校验必须由程序完成：拿到模型输出后，逐条检查 `sourceId` 是否真实存在于本次检索到的来源列表中，不存在的直接降级为 `unknown`，而不是信任模型。

### 3.6 预置静态结果（演示可靠性的底线）

v1 把"示例任务"排在第三天当锦上添花，这是错的。

面试官点开链接时，可能正好赶上你当月 Tavily 额度用尽、Gemini 限流、或某个产品官网改版抓取失败。

**演示必须是零 API 调用也能完整展示的。**

做法：

- 跑通一次真实流程后，把返回的 JSON 原样存进 `fixtures/demo-report.json`；
- 创建页放一个显眼的「查看示例报告」按钮，直接读取该 fixture，不触发任何 API；
- 报告页只依赖 JSON 结构，因此**示例报告和真实报告走完全相同的渲染路径**，不会出现"示例好看、真实版崩了"。

### 3.7 两个 v1 遗留的逻辑矛盾

**（a）「允许第三方来源」默认值**

v1 默认关闭，但**不填官网时，产品信息就只可能来自第三方来源**——两个设置会打架，结果是"无官网 + 默认设置 = 该产品无解"。

修正：默认**开启**，并在 UI 上说明理由（"不填官网时需要第三方来源才能找到信息"）。若用户主动关闭，则无官网的产品直接标记为 `信息不足` 并说明原因，而不是静默失败。

**（b）`localStorage` 容量**

5 个产品 × 十维 × 证据数组的 JSON 很容易超过 localStorage 的 ~5MB 上限，塞不进去，v1 的验收项"页面刷新后能恢复最近一次结果"大概率不通过。

修正：localStorage 只存轻量任务元数据（产品名、状态、时间戳）；报告正文依赖 §3.6 的 fixture。若后续确实需要持久化完整报告，改用 IndexedDB。

### 3.8 核心接口

`POST /api/research/product`

```json
{
  "name": "产品名称",
  "officialUrl": "可选官网",
  "allowThirdParty": true
}
```

输出包括产品基本信息、十维字段、证据数组、信息缺口和错误状态。

`POST /api/research/report`

输入为 3–5 个产品的结构化调研结果，输出总结、差异化洞察、适用用户、市场机会、图表数据和待验证事项。

前端分别调用产品接口，避免单个超长请求导致整份报告失败。

> **实现后的修正**：产品调研改为**完全串行**（`research.ts` 里的普通 `for` 循环），不再设并发上限 2。免费层的限流是按 credit 计的，并发只会换来 429 风暴而不是更快的报告；串行还顺带简化了前端——一次只等一个产品，重试也就只需重跑那一个。

## 4. 排期：4–5 天

v1 的 2–3 天排期偏乐观。逐项核对后，第一天塞进了 4 个独立难点（项目搭建、Tavily 接入、结构化抽取 Schema 设计、来源绑定校验），其中**来源绑定**单独就要磨半天到一天；第二天同样是满负荷。

改为 4–5 天，或按 3 天排但明确砍掉"移动端适配"（§1 已声明用户主要在电脑端，与 v1 第三天任务自相矛盾）。

### 第一天：跑通真实调研链路

- Next.js 项目搭建、Tailwind、环境变量模板；
- 产品输入、官网可选字段及校验；
- **用内置 WebSearch 先验证「搜索 → 抽取」Prompt**（不烧 Tavily 额度）；
- 接入 Tavily 搜索与 Gemini 结构化抽取；
- 定义统一数据结构、来源绑定和 `unknown` 状态；
- 用一款 AI 客服产品完成端到端验证。

### 第二天：多产品调研 + 来源绑定加固

- 5 个产品合并为 1 次模型调用的抽取逻辑；
- zod 校验 + 编造 `sourceId` 的降级处理；
- 429 指数退避重试；
- 并发上限 2、进度状态、失败提示、单项重试。

### 第三天：横评报告

- **热力矩阵**（十维四态，含图标 + 文字，色盲安全）；
- 证据覆盖率条形图；
- 定价图 / 定价模式表的条件分支；
- 事实、AI 推断、待确认三级内容标记；
- 选定一组标准演示案例并人工核查。

### 第四天：作品集化与交付

- **预置静态示例报告 + 无 Key 可看的入口**（§3.6，优先级最高）；
- 页面层级、加载态、空状态；
- 部署 Vercel 公开演示页；
- 保存产品流程图、关键页面截图和测试结果。

### 第五天（缓冲）

- 整理项目背景、产品取舍、AI 工作流、风险治理与复盘材料；
- 补验收项中未覆盖的部分。

## 5. 测试与验收

必须通过：

- 输入 3 款有效产品，可以生成完整报告；
- **无 API Key 时，点击「查看示例报告」能完整展示报告页**；
- 未填写官网时能通过搜索找到候选来源；
- 同名或来源不确定时显示待确认，不自动冒认官网；
- 模型返回不存在的 `sourceId` 时，该条事实被降级为 `unknown` 而非展示；
- 搜索或模型接口失败时显示可理解错误，并允许重试；
- 429 限流时自动退避重试，前端有明确提示；
- 一个产品失败不影响其他产品完成；
- 每条"已核实事实"都有可点击来源；
- 没有公开定价时不生成虚假价格；
- 图表只使用结构化数据，不解析模型生成的 HTML；
- **热力矩阵的四种状态在不依赖颜色的情况下仍可区分**（图标 + 文字）；
- 页面刷新后能恢复最近一次任务状态；
- API Key 不出现在浏览器代码、报告或日志中；
- 单产品链路在 Vercel Hobby 上不触发 300 秒超时；
- 标准演示案例由人工抽查至少 20 条关键事实并记录正确率。

## 默认假设

- 项目定位为求职作品集 MVP，而非商业化 SaaS；
- 用户主要在电脑端使用中文界面；
- 输入支持任意产品，但只对一组 AI 客服产品承诺演示质量；
- 官网优先；第三方来源默认开启（§3.7a）；
- 首版报告不可编辑、不支持继续对话；
- 不直接生成和执行任意 HTML，所有页面由可信组件渲染；
- 免费额度政策可能变化，正式开发前以服务商控制台显示为准。

## 附：成本

**总计 ¥0。**

| 项目 | 方案 | 成本 |
|---|---|---|
| 部署 | Vercel Hobby | ¥0（`xxx.vercel.app` 子域名） |
| 搜索 | Tavily 免费层 1000 credits/月 | ¥0 |
| 大模型 | Gemini 免费层 | ¥0 |
| 数据库 | 不建，用静态 fixture | ¥0 |
| 域名 | 不购买，子域名对作品集足够 | ¥0 |

备用金：若超出免费额度，gemini-2.5-flash 约 $0.30/百万输入 token，单次完整报告约 100–300k token ≈ **¥0.2–0.7 / 次**。**¥20 可覆盖 30 次以上**，随用随付。
